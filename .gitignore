.aptos-core/
.mvlite-session/
.movelite-session/
target/
build/
node_modules/
/docs

# Local-only internal docs, never published
AUDIT_FINDINGS.md
POSTMORTEM-*.md

# Kept local, not published to the public repo: AI guidelines + the TS
# verification harness (not wired into CI; CI runs `cargo test` in src/).
CLAUDE.md
test/
